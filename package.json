{
  "name": "pos-saas",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-Tenant POS SaaS - monorepo root",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
